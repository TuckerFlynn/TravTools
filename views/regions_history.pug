extends layout

block script
  script(type='text/javascript' src='/javascripts/region_history.js')

block content
  div.row
    div.col-sm-12
      h1= title
      hr
  div.row
    div.col-sm-12
      p Select one or more alliances to view region history. 
      | Selecting alternate alliances allows choosing disbanded alliances that were in the top 5 of a region at some point.
      form#form(method='POST' action='')
        table.table.table-sm#aTable
          tr
            td
              label(for='alliance') Alliance:
            td#aDefault
              select#alliance.form-control
                option(value=0) -

                each val, index in alliances
                  option(value=alliances[index].aID)= alliances[index].Alliance
          tr
            td
              button.btn.btn-warning#all Alt Alliances
            td
              button.btn.btn-primary#add Add Alliance
            td
              button.btn.btn-danger#remove Remove Alliance
            td
              button.btn.btn-success(type='submit') Submit